# MI RNA21 — Strategic Matrix Gate
DATE: 2026-09-21
MODE: INTELLIGENCE -> MATRIX EXECUTION GATE

## Role separation

The scientific controller (this document + forensic audit) defines only admissible questions, falsification logic, controls, and stop conditions that are already supported by project evidence.

The Simulation Matrix is the execution engine. It may enumerate millions of parameter/seeds/world combinations inside a frozen contract, but it may not invent a missing scientific contract.

## Current strategic result

No new scientific simulation is admissible yet.

The highest-value unresolved operation is not Monte Carlo volume. It is exact provenance recovery for the frozen R8D representation and its executable V1 mapping/null.

Reason:
V1 requires an exact frozen transform, feature order, dimensions, and signal-to-feature mapping. Without those, millions of simulations would test a model chosen by us rather than the registered representation.

## Matrix-ready experiment queue

### E0 — Provenance resolution gate
Question: Can the exact frozen R8D artifact, feature order, dimensions, transformation lineage, executable V1 mapping, and null implementation be recovered from authoritative evidence?
PASS: all required fields have exact artifact/version/hash/lineage.
FAIL: any field remains unresolved.
STOP: scientific execution remains closed.

### E1 — V1 Level-10 contract sanity
Eligible only after E0 PASS.
Question: Does the frozen pipeline execute without leakage and with a valid null?
Primary checks: train/test write-once, null independence, finite CE/IR denominator, reproducibility.
STOP on any validity failure.

### E2 — V1 scale escalation
Eligible only after E1 PASS.
Levels: 100 -> 1,000 -> 10,000 -> 100,000 -> 1,000,000.
Escalate only when all validity gates and predefined PASS criteria survive.

### E3 — Frozen robustness grid
Eligible only after E1 PASS.
Use the already registered ds/dw grid. This is robustness testing, not threshold tuning.

### E4 — Historical V5 reproduction
Independent branch of the matrix.
Eligible only after exact V5 executable recovery.
First reproduce historical checkpoints unchanged; only then run the predeclared adversarial suite.

### E5 — Biological validation
Not part of V1 synthetic execution.
TCGA remains cohort-specific retrospective evidence.
WHEL remains HOLD until authoritative Endpoints.csv/version provenance passes.

## Forbidden matrix behavior

- inventing R8D coefficients
- inventing feature order or dimensions
- choosing a null definition after seeing results
- changing metric/threshold after results
- using V3/V4/V5 to fill V1 gaps
- substituting WHEL endpoints
- treating synthetic success as biological validation

## Decision output

The matrix must return one of:
1. SCIENTIFIC-READY: all contracts bound and experiment passes validity gate.
2. SCIENTIFIC-FAIL: a frozen experiment fails its predefined validity/scientific criterion.
3. NON-IDENTIFIABLE: target and artifact are observationally indistinguishable under the frozen measurement model.
4. INCONCLUSIVE-BLOCKED: required provenance/contract is missing.

Current status: INCONCLUSIVE-BLOCKED.
Current justified stop: E0.
