# R8D Frozen Representation

Status: PROVENANCE REQUIRED / EXECUTION BLOCKED

R8D is the current 8-dimensional multi-task PLS candidate using PAM50 + miR-21 task information.

The repository repair does not recreate or approximate the transform.

Required before V1 execution:

- exact transform artifact
- exact feature order
- exact training/provenance dataset identity
- version/hash
- fit lineage
- executable loading path
- deterministic transformation test

If any of these cannot be recovered exactly, the Simulation Matrix must stop rather than invent a substitute.
